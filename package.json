{
    "name": "ai-kanban",
    "version": "1.0.0",
    "description": "AI-powered Kanban board with intelligent task management",
    "scripts": {
        "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
        "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install"
    },
    "keywords": [
        "kanban",
        "ai",
        "project-management"
    ],
    "author": "",
    "license": "ISC",
    "devDependencies": {
        "concurrently": "^8.2.2"
    }
}