{
    "name": "specflow-ai",
    "version": "1.0.0",
    "description": "SpecFlow AI - Intelligent Kanban board with automated PRD generation",
    "scripts": {
        "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
        "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install"
    },
    "keywords": [
        "kanban",
        "ai",
        "project-management"
    ],
    "author": "",
    "license": "ISC",
    "devDependencies": {
        "concurrently": "^8.2.2"
    }
}